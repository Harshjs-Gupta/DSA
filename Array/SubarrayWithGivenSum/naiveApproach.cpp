#include <iostream>
using namespace std;

int maxSum(int arr[], int n, int k)
{
}

int main()
{
    int arr[1000], n, res, sum;
    cout << "Enter the number of elemenst: ";
    cin >> n;
    cout << "Enter the elements: ";
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    cout << "Enter the number of sum: ";
    cin >> sum;

    return 0;
}